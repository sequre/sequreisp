<% content_for :title, t('actions.new', :model => AvoidBalancingHost.human_name) %>
<% heading t('actions.new', :model => AvoidBalancingHost.human_name)  %>

<%= render :partial => "shared/empty_submenu" %>

<%= render @avoid_balancing_host %>
