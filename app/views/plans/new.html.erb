<% content_for :title, t('actions.new', :model => Plan.human_name) %>
<% heading t('actions.new', :model => Plan.human_name) %>

<%= render :partial => "shared/empty_submenu" %>

<%= render @plan %>

