<% content_for :title, t('actions.edit', :model => ProviderGroup.human_name) %>
<% heading t('actions.edit', :model => ProviderGroup.human_name) %>

<ul class="nav" style="float:right">
  <li><%= link_to t('buttons.view_audit'), audits_path('search[auditable_type_is]' => 'ProviderGroup', 'search[auditable_id_equals]' => @provider_group.id) %> </li>
  <%= render :partial => "shared/submenu_items"%>
</ul>
<div style="clear: both"></div>

<%= render @provider_group %>

