<% content_for :javascript do %>
  <%= javascript_include_tag "highstock" %>
  <%= javascript_include_tag "exporting" %>
<% end %>

<script type="text/javascript">
  $(function() {
    $("#tabs").tabs();
  });

  function get_data(){
    $.get('<%= instant_provider_group_path %>', null, function(data) {
      instant.series[0].addPoint(data.rx, true, true);
      instant.series[1].addPoint(data.tx, true, true);
    },'json');
  };

  $(document).ready(function() {
    instant = new Highcharts.Chart(<%= @graphs["provider_group_instant"].graph! %>);
    <% @periods.times do |i| %>
      new Highcharts.StockChart(<%= @graphs["provider_group_period_#{i}"].graph! %>);
    <% end %>

    get_data();

    setInterval(function() {
      get_data();
    }, 5000);
  });
</script>

<%= heading "#{@provider_group.name} #{@provider_group.state}" %>
<% content_for :title, @provider_group.name %>

<div id=<%= @graphs["provider_group_instant"].render %> style="height:247px; width:860px; float:left;"></div>
<div class="separador" style='margin-bottom: 5px;'></div>

<% content_tag(:div, "", :id => "tabs") do %>
  <ul>
    <% @periods.times do |period| %>
      <li><a href=<%="#tabs-#{period}"%>><%= t("graphs.titles.tabs.interfaces.period_#{period}") %></a></li>
    <% end %>
  </ul>

  <% @periods.times do |period| %>
    <% content_tag(:div, "", :id => "tabs-#{period}" ) do %>
      <div id=<%= @graphs["provider_group_period_#{period}"].render %> style="height:350px; width:830px; float:left;"></div>
      <div class="separador" style='margin-bottom: 5px;'></div>
    <% end %>
  <% end %>
<% end %>
