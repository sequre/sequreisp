<% content_for :title, t('actions.new', :model => ProviderGroup.human_name) %>
<% heading t('actions.new', :model => ProviderGroup.human_name) %>

<%= render :partial => "shared/empty_submenu" %>

<%= render @provider_group %>

