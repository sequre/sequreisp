<% content_for :title, t('actions.new', :model => User.human_name) %>
<% heading t('actions.new', :model => User.human_name) %>

<%= render :partial => "shared/empty_submenu" %>

<%= render @user %>

